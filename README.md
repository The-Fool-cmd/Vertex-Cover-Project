### **Tests**
Tests are generated determinstically in **generate_tests.py** and placed in the **tests/** folder.

---

### **Algorithms**
Algorithms are implemented, and benchmarked, in **main.cpp**.

---

### **Compilation**
Run `make help` for a list of rules.

---

### **Results**
Results can be found in `[name]`.csv, check make rules above.
Results are also discussed in main.tex.

---

### **External Assistance**
Ai for help with the tests generating script and algorithm implementations.